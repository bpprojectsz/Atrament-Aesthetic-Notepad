<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Baseline: AdMob banner loading (every app) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Daily verse reminder notifications (Section 7) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Face ID / fingerprint note lock (Section 1) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
      Deliberately NOT declared (Section 8):
      - RECORD_AUDIO — no audio recording feature
      - CAMERA — uses system photo picker if ever added, no direct capture
      - READ_EXTERNAL_STORAGE / READ_MEDIA_IMAGES — file_picker delegates to OS document UI
      - WRITE_EXTERNAL_STORAGE — share_plus hands off to the system share sheet
    -->

    <application
        android:label="Atrament"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!--
              LaunchTheme (styles.xml, generated by flutter_native_splash)
              shows the branded splash background until Flutter draws its
              first frame, then this meta-data swaps to NormalTheme so the
              splash background doesn't persist behind the real UI.
            -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Required by flutter_local_notifications for exact-time scheduling -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
          AdMob application ID (Section 1a).
          REPLACE: ca-app-pub-xxxxxxxx~xxxxxxxx with your real AdMob
          Android app entry before submission. The value below is
          Google's official test application ID, safe for development
          builds only.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

    </application>
</manifest>
