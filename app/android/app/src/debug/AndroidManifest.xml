<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter needs it to communicate with the running application to
         allow setting breakpoints, to provide hot reload, etc. This
         permission is merged only into debug builds, never release. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
